name: file_organizer_cli
description: select files in a directory and map them
version: 0.0.1
repository: https://github.com/FMotalleb/file_organizer_cli

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.1
  cli_completion: ^0.3.0
  mason_logger: ^0.2.5
  meta: ^1.9.1
  path: ^1.8.3
  pub_updater: ^0.3.0
  yaml: ^3.1.2

dev_dependencies:
  build_runner: ^2.4.4
  build_verify: ^3.1.0
  build_version: ^2.1.1
  mocktail: ">=0.3.0 <2.0.0"
  test: ^1.24.2
  very_good_analysis: ^5.0.0

executables:
  organize: file_organizer_cli
